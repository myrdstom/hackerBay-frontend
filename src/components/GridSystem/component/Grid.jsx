import React from 'react';

const Grid = (rows, columns) => {

    let cell = [];
        cell.push(
            <div className="square" />
        )

    return (
        <div>
            {Grid}
        </div>
    );
};

export default Grid;
